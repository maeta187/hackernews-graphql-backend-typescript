type Link {
  id: ID!
  description: String!
  url: String

type User {
  id: ID!
  name: String!
  email: String!
  links: [Link!]!
}

}

type Query {
  feed: [Link]
}

type Mutation {
  post(url: String!, description: String!): Link!
}
